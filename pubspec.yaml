name: finanapp
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  intl: ^0.20.2
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  path_provider: ^2.1.3
  
  # BLoC State Management
  flutter_bloc: ^8.1.6
  equatable: ^2.0.5
  
  # Other dependencies
  animated_list_plus: ^0.5.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  hive_generator: ^2.0.1
  build_runner: ^2.4.6
  
  # BLoC Testing
  bloc_test: ^9.1.6
  mocktail: ^1.0.0

flutter:
  uses-material-design: true
  
  assets:
    - assets/images/porquinho_triste.png
    - assets/images/porquinho_neutro.png
    - assets/images/porquinho_feliz.png